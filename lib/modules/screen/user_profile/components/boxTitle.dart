import 'package:flutter/material.dart';

Widget boxTitle(
  String title,
) {
  return Container(
    child: Text(
      title,
      style: TextStyle(fontSize: 16.0, fontWeight: FontWeight.bold),
    ),
  );
}
